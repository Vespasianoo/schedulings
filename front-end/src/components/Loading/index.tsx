import { Container, LoadingBar } from "./styles"

export const Loading = () => {
  return (
    <Container>
      <LoadingBar></LoadingBar>
      <LoadingBar></LoadingBar>
      <LoadingBar></LoadingBar>
      <LoadingBar></LoadingBar>
    </Container>
  )
}
